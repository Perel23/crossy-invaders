#pragma once
#include <SDL3/SDL.h>
#include "bagel.h"

namespace ci
{
	// Components will be defined here in Step 2.
	// Each component is a plain struct; storage type is chosen via
	// template specialization of bagel::Storage<T> (see Pong.h for examples).
}
